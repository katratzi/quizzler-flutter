class Question {
  String questionText;
  bool questionAnswer;

  Question(this.questionText, this.questionAnswer);
  // if we wanted the paramnters to be named, just add in {}
  //Question({this.questionText, this.questionAnswer});
}
